[user]
	email = thomas.wygonik@gmail.com
	name = twaluigi
[core]
	editor = vim
[push]
	default = simple
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
